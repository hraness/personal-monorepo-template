# Third-party software

This template uses public Hraness projects under their own MIT licenses:

- [`@hraness/ui`](https://github.com/hraness/ui), pinned to `v0.2.0`.
- [`@hraness/direct`](https://github.com/hraness/direct), pinned to `v0.6.1`.
- [`@hraness/kb`](https://github.com/hraness/kb), pinned to `v0.14.0`.

The `query-kb`, `refresh-kb`, `percolate-kb`, `plan-kb`, `save-url-kb`,
and `save-pdf-kb` skill directories are adapted from the public `hraness/kb`
project. The `direct-setup` and `direct-verify` skill directories come from
the public `hraness/direct` project. Their copyright and license remain
governed by those projects' MIT licenses.

The local personal-site styles, writing guides, reasoning skills, riff
workflow, disk-reclamation workflow, phased-work workflow, and merge serializer
are distributed under this repository's MIT license. Installed transitive
dependencies retain their respective licenses.
