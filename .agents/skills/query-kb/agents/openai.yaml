interface:
  display_name: "Query a Knowledge Base"
  short_description: "Load scoped context, then search with visible evidence"
  default_prompt: "Use $query-kb to load repository-path context, then retrieve the smallest useful note set with each evidence lane kept distinct."
